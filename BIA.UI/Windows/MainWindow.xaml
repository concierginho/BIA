<Window x:Class="BIA.UI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BIA.UI.Windows"
        mc:Ignorable="d"
        Title="BIA.UI"
        SizeToContent="WidthAndHeight">
   <Window.Resources>
      <Style TargetType="Button">
         <Setter Property="Margin" Value="3, 3, 0, 2"/>
      </Style>
   </Window.Resources>
   <Grid Margin="5">
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="200"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
         <RowDefinition/>
         <RowDefinition/>
         <RowDefinition/>
         <RowDefinition/>
      </Grid.RowDefinitions>
      <Button Grid.Column="0" Grid.Row="0" Command="{Binding InitializeBIACommand}" Content="Initialize"/>
      <Button Grid.Column="0" Grid.Row="1" Command="{Binding StartBIAProcessCommand}" Content="Prepare Process"/>
      <Button Grid.Column="0" Grid.Row="2" Command="{Binding StartOperationProcessCommand}" Content="Start Operations"/>
      <Button Grid.Column="0" Grid.Row="3" Command="{Binding StopBIAProcessCommand}" Content="Stop"/>
   </Grid>
</Window>
